# تقرير فحص مسار خروج الرسائل والعمليات المرافقة له

## 1. الهدف من التقرير
تهدف هذه الوثيقة إلى توثيق ما تم تنفيذه فعليًا في مسار خروج الرسائل من المنصة، من لحظة الضغط على زر الإرسال حتى انتهاء العملية وتحديث الحالة في قاعدة البيانات.

## 2. الخلاصة المختصرة
تم بناء مسار خروج رسائل منظم على النحو التالي:
1. يتم جمع الرسائل من الواجهة.
2. يتم حفظ الحملة والرسائل أولًا في قاعدة البيانات عبر Edge Function مخصص.
3. ثم يتم استدعاء دالة الإرسال المناسبة (SMS أو Email) باستخدام معرف الحملة المحفوظ.
4. يتم تحديث حالة الحملة والرسائل والجهود/الأحداث المرتبطة بها بعد نهاية الإرسال.

هذا يعني أن المنطق الحالي يحقق فصلًا واضحًا بين "الحفظ" و"الإرسال"، وهو ما يتوافق مع المتطلبات الأساسية لمسار الإرسال الموثوق.

## 3. التدفق الحالي في النظام

### 3.1 من الواجهة إلى الخلفية
الواجهة الرئيسية التي تتحكم في هذه العملية هي [src/user/components/dashboard/NewCampaignDialog.tsx](src/user/components/dashboard/NewCampaignDialog.tsx).

العملية الحالية تتم على النحو التالي:
- يتم تحويل جهات الاتصال إلى قائمة رسائل.
- يتم بناء payload للحفظ باسم "save-campaign".
- يتم استدعاء Edge Function لحفظ الحملة والرسائل.
- إذا نجح الحفظ، يتم استخراج campaign_id من الاستجابة.
- بعد ذلك يتم إرسال الطلب الثاني إلى Edge Function الخاص بالإرسال: "send-sms" أو "send-email" مع campaign_id.

### 3.2 دالة الحفظ
تمت إضافة/استخدام دالة الحفظ في [supabase/functions/save-campaign/index.ts](supabase/functions/save-campaign/index.ts).

وظائفها الحالية:
- التحقق من وجود رسائل صالحة.
- إنشاء سجل حملة جديد في جدول campaigns.
- إدراج الرسائل في جدول campaign_messages.
- إرجاع campaign_id إلى الواجهة.

وهذا يحقق أول خطوة أساسية في المسار: حفظ الرسائل قبل الإرسال.

### 3.3 دالة الإرسال عبر SMS
تمت معالجتها في [supabase/functions/send-sms/index.ts](supabase/functions/send-sms/index.ts).

الخطوات التي تتم فيها:
- التحقق من المستخدم والتفويض.
- التحقق من مفتاح API الخاص بالمستخدم.
- التحقق من صحة الأرقام والرسائل.
- التحقق من حدود الإرسال (rate limit).
- استخدام campaign_id إن كان موجودًا، أو إنشاء حملة جديدة إن لم يكن موجودًا.
- حفظ الرسائل في campaign_messages إذا لم تكن محفوظة مسبقًا.
- إنشاء سجلات في delivery_attempts و delivery_events.
- استدعاء مزود الرسائل الخارجي (hudhud) عبر Adapter.
- تحديث حالة الحملة والرسائل بعد الإرسال.
- تسجيل تفاصيل الإرسال في sms_logs.
- إرسال إشعار push عند النجاح إلى الأجهزة المسجلة.

### 3.4 دالة الإرسال عبر البريد الإلكتروني
تمت معالجتها في [supabase/functions/send-email/index.ts](supabase/functions/send-email/index.ts).

الخطوات التي تتم فيها:
- التحقق من المستخدم والتفويض.
- التحقق من توفر مفتاح Resend API.
- التحقق من صحة الرسائل.
- التحقق من حدود الإرسال.
- إنشاء أو استخدام حملة محفوظة.
- حفظ الرسائل في campaign_messages عند الحاجة.
- إنشاء سجلات delivery_attempts و delivery_events.
- إرسال البريد عبر Resend.
- تحديث حالة الحملة والرسائل بناءً على نتيجة الإرسال.

### 3.5 التعديل الذي أضيف إلى الواجهة: صفحة "وسائط الإرسال"
تمت إضافة طبقة جديدة من الواجهة تعكس التعديل الذي أُجري على تجربة المستخدم والإدارة، وهي صفحة "وسائط الإرسال" التي أصبحت جزءًا من المسار العام للرسائل، من خلال:
- إضافة عنصر تنقل جديد في [src/user/components/dashboard/DashboardLayout.tsx](src/user/components/dashboard/DashboardLayout.tsx).
- ربط المسار الجديد في [src/user/App.tsx](src/user/App.tsx) لعرض الصفحة للمستخدم.
- إنشاء صفحة المستخدم في [src/user/components/dashboard/SendingChannels.tsx](src/user/components/dashboard/SendingChannels.tsx) لعرض الشركات/الجهات المسموح بها.
- إضافة عنصر تنقل إداري في [src/admin/components/SuperAdminLayout.tsx](src/admin/components/SuperAdminLayout.tsx).
- ربط المسار الإداري في [src/admin/AdminApp.tsx](src/admin/AdminApp.tsx).
- إنشاء شاشة الإدارة في [src/admin/components/SendingChannelsAdmin.tsx](src/admin/components/SendingChannelsAdmin.tsx) لإدارة النطاقات/الشركات المسموح بها.

المقصود من هذا التعديل هو تمكين الإدارة المركزية لوسائط الإرسال من خلال قائمة الشركات/النطاقات المسموح بها، ثم عرضها للمستخدمين كخيارات قابلة للاختيار، مع الاستفادة من Edge Function [supabase/functions/manage-company-domains/index.ts](supabase/functions/manage-company-domains/index.ts) لتغذية الواجهة من قاعدة البيانات.

هذا التعديل لا يغيّر منطق الإرسال الأساسي نفسه، لكنه يضيف طبقة واجهة وبيانات جديدة تدعم مستقبل الربط الحقيقي مع قنوات الإرسال الخارجية أو الشركات المصرح لها بالاتصال.

## 4. الجداول والبيانات التي يتم تحديثها أثناء العملية

### 4.1 campaigns
تُستخدم لتخزين معلومات الحملة الأساسية مثل:
- اسم الحملة
- حالة الحملة
- عدد جهات الاتصال
- مصدر الحملة
- حالة final مثل completed أو failed

### 4.2 campaign_messages
تُستخدم لتخزين كل رسالة داخل الحملة، مع معلومات مثل:
- رقم الهاتف
- النص
- الحالة الحالية مثل pending أو sent أو failed
- timestamps الإرسال
- خطأ الإرسال عند وجوده

### 4.3 delivery_attempts
تُستخدم لتسجيل كل محاولة إرسال مخصصة لكل رسالة، وتشمل:
- campaign_message_id
- provider
- channel
- status
- attempts
- response_data
- error_message

### 4.4 delivery_events
تُستخدم لتسجيل الأحداث التي تطرأ خلال الإرسال مثل:
- dispatch_started
- sent
- failed

### 4.5 sms_logs
تُستخدم لتسجيل ملخص الإرسال النهائي عند استخدام SMS، وتشمل:
- user_id
- api_key_id
- recipients_count
- status
- response_data

## 5. ما تم تحقيقه فعليًا

### ✅ مكتمل جزئيًا أو كليًا
- فصل عملية الحفظ عن عملية الإرسال.
- حفظ الحملة والرسائل قبل الإرسال.
- تحديث حالة الحملة بعد الانتهاء.
- تحديث حالة الرسائل بعد الانتهاء.
- تسجيل محاولات الإرسال في delivery_attempts.
- تسجيل أحداث الإرسال في delivery_events.
- دعم SMS و Email كقنوات منفصلة.
- دعم التحقق من حدود الإرسال.

## 6. النقاط التي تحتاج تحسينًا أو توثيقًا إضافيًا

### 6.1 لا يوجد retry policy حقيقي بعد الفشل
حاليًا يتم تسجيل محاولة واحدة فقط، ولا توجد منطق إعادة محاولة متقدم يعتمد على أنواع الأخطاء أو الفواصل الزمنية.

### 6.2 لا يوجد queue worker مستقل
العملية الحالية تتم في نفس الطلب HTTP، وهي مناسبة للاستخدام الأولي لكنها ليست مثالية للتحميل العالي أو الاستمرارية.

### 6.3 لا يوجد webhook handler
لم يتم ربط مسار الإرسال مع استلام تحديثات الحالة من مزود خارجي بشكل منفصل.

### 6.4 لا يوجد نظام idempotency كامل
على الرغم من وجود delivery_attempts، فلا يوجد آلية كاملة لمنع التكرار عند إعادة المحاولة أو إعادة التشغيل.

### 6.5 الإرسال عبر البريد الإلكتروني يرسل رسالة واحدة مركبة للمستخدم
هذا مناسب للاختبار الأولي، لكنه لا يعكس بعد نموذج إرسال فردي لكل مستلم في كل رسالة منفصلة.

## 7. التقييم العام
المنصة وصلت إلى مرحلة جيدة من التهيئة لمسار خروج الرسائل، خاصة من ناحية:
- التخزين المسبق للحملة والرسائل
- التتبع في قاعدة البيانات
- فصل منطق الحفظ عن منطق الإرسال
- تسجيل حالات الإرسال

لكنها ما تزال تحتاج إلى تحسينات تشغيلية قبل اعتبارها نظامًا إنتاجيًا بالكامل، خصوصًا فيما يتعلق:
- إعادة المحاولة
- الطابور/المعالجة غير المتزامنة
- التتبع الخارجي عبر webhooks
- إدارة الأخطاء على مستوى الرسائل الفردية

## 8. التوصية النهائية
الخطوة القادمة الأنسب هي:
1. إضافة نظام إعادة محاولة ذكي لكل delivery_attempt.
2. تحويل العملية من "طلب مباشر" إلى "queue-based worker".
3. ربطها ب webhook أو callback من المزود.
4. توسيع سجلات الحالة لتشمل حالات مثل queued و rejected و expired.

## 9. الخلاصة
تم تنفيذ أساس قوي لمسار خروج الرسائل، ويعمل حاليًا على مستوى التخزين، التتبع، والتعامل مع الطلبات المباشرة بشكل مناسب للاستخدام الأساسي. ومع ذلك، لا يزال يتطلب تطويرًا إضافيًا ليصبح نظامًا جاهزًا للإنتاج على نطاق أكبر.
