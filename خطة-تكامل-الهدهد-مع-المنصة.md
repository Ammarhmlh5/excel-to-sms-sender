# خطة تكامل Hudhud مع المنصة

## 1. الهدف
إنشاء اتصال كامل بين المنصة و Hudhud يسمح بإرسال الرسائل واستقبال الردود أو الأوامر بشكل موثوق، مع حفظ الحالة في قاعدة البيانات وتحديثها تلقائيًا في الواجهة.

## 2. المتطلبات الأساسية
قبل البدء يجب توفر العناصر التالية:

### 2.1 بيانات الاعتماد
- مفتاح API أو Token خاص بالعميل
- اسم المستخدم أو الحساب المرتبط بالعميل
- بيانات التسجيل الخاصة بالعميل في Hudhud
- عنوان endpoint الأساسي الخاص بالخدمة
- إن وجد: Secret Key أو Signature أو Shared Secret

### 2.2 الإعدادات التقنية
- وصول HTTPS
- دعم JSON أو Form-data حسب واجهة المزود
- إعدادات Timeout
- آلية Retry عند الفشل
- تسجيل الأخطاء والردود الواردة

## 3. المتطلبات العملية للإرسال
لضمان إرسال ناجح يجب أن تكون المنصة قادرة على:
- استقبال رقم المستلم
- استقبال نص الرسالة
- تحديد اسم المرسل أو Sender ID
- إرسال معرف مرجعي لكل رسالة
- استقبال نتيجة الإرسال من Hudhud
- حفظ الحالة في قاعدة البيانات

## 4. المتطلبات العملية للاستقبال
لضمان استقبال كامل يجب توفر:
- Webhook URL داخل المنصة
- طريقة استقبال عبر POST
- مصادقة الطلبات القادمة من Hudhud
- آلية تأكيد الاستلام عبر 200 OK
- تخزين الرسائل الواردة في قاعدة البيانات
- تحويل الرسائل الواردة إلى أوامر أو أحداث داخل المنصة

## 5. البنية المقترحة
### 5.1 Backend
نقترح إضافة طبقة خدمة داخل المشروع مسؤولة عن:
- إرسال الرسائل إلى Hudhud
- استقبال Webhook من Hudhud
- تحديث حالات الرسائل
- تسجيل الأخطاء والردود

### 5.2 قاعدة البيانات
يُفضّل إضافة الجداول التالية:
- hudhud_connections: لتخزين إعدادات الاتصال لكل عميل
- outbound_messages: لتخزين الرسائل الصادرة
- inbound_messages: لتخزين الرسائل الواردة
- delivery_events: لتسجيل تغييرات الحالة
- hudhud_logs: لتسجيل الأخطاء والردود

## 6. التدفق المقترح
1. المستخدم ينشئ حملة أو رسالة جديدة.
2. المنصة تحفظ الرسالة في قاعدة البيانات.
3. المنصة ترسل الطلب إلى Hudhud عبر API.
4. Hudhud يعيد نتيجة الإرسال.
5. المنصة تحفظ الحالة: pending / sent / failed / delivered / rejected.
6. إذا وصل Webhook من Hudhud، يتم تحديث الحالة تلقائيًا.

## 7. الإعدادات المطلوبة داخل المشروع
يجب أن تكون هناك إعدادات مركزية مثل:
- Hudhud API Key
- Base URL
- Webhook URL
- Sender ID
- Timeout
- Retry Count
- Enable/Disable Webhook

## 8. الخطوات التنفيذية
1. إضافة إعدادات Hudhud في البيئة العامة أو ملف الإعدادات.
2. إنشاء Edge Function أو endpoint لإرسال الرسائل.
3. إنشاء Edge Function أو endpoint لاستقبال Webhook.
4. إنشاء الجداول اللازمة في Supabase.
5. ربط عملية الإرسال الحالية بالـ Hudhud بدل المزود الحالي.
6. إضافة منطق تحديث الحالة بناءً على الاستجابة والـ Webhook.
7. اختبار إرسال رسالة واحدة كاملة من البداية إلى النهاية.
8. اختبار استقبال رسالة أو رد من Hudhud.
9. التأكد من تسجيل الأخطاء والردود بشكل واضح.
10. تفعيل المراقبة والتتبع في لوحة الإدارة.

## 9. ما يجب أن يكون جاهزًا قبل التشغيل الفعلي
- مفتاح API صالح
- عنوان Webhook قابل للوصول عبر الإنترنت
- صلاحية HTTPS
- جدول قاعدة بيانات جاهز
- منطق معالجة الأخطاء
- نظام تتبع للحالات

## 10. الخلاصة
التكامل مع Hudhud لا يعتمد فقط على وجود المفتاح، بل يحتاج إلى بنية كاملة تشمل الإرسال والاستقبال، قاعدة بيانات، Webhook، ومراقبة الأخطاء. عند اكتمالها تصبح المنصة قادرة على العمل كموزع رسائل موثوق عبر Hudhud.
