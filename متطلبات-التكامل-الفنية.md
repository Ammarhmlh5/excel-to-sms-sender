# وثيقة المتطلبات التقنية والفنية
## تطبيق هدهد موبايل — Universal SMS Gateway
## ربط أي منصة SMS بأي جهاز جوال

**النسخة:** 2.0 — Multi-Platform Gateway  
**التاريخ:** 9 يوليو 2026

---

## 1. الرؤية العامة

### 1.1 المفهوم

تطبيق هدهد موبايل يتحول من تطبيق مخصص لمنصة الهدهد إلى **طبقة وسيطة (Soil)** بين أي منصة إرسال رسائل وأي جهاز مستخدم.

```
[منصة الهدهد]  ─┐
[مرسال Excel]   ─┤
[منصة مخصصة 1]  ─┼── [Supabase Events Table] ←── [تطبيق جوال] ←── [SIM / API]
[منصة مخصصة 2]  ─┤
[أي منصة أخرى]   ─┘
```

### 1.2 مبدأ المصادقة — "الثقة المشتركة بالتوكن"

يعمل مثل GitHub ↔ Supabase:  
كل منصة تصدر توكنها الخاص، وكل منصة تثق في التوكن عبر التوقيع الرقمي — **لا استعلامات API بين الأنظمة**:

```
[المستخدم] ← يسجل دخول ← [المنصة س]
  │
  │ المنصة ترخص التوكن
  │ التطبيق يخزن التوكن محلياً
  │
  └──→ التطبيق يستخدم التوكن لكل عملية لاحقة
```

### 1.3 الأطراف المتكاملة

| الطرف | الدور | التقنية |
|-------|-------|---------|
| **تطبيق هدهد موبايل** | Universal Gateway | Expo + Supabase SDK + SQLite |
| **منصة الهدهد** | إحدى المنصات المزودة للرسائل | Go/Gin + PostgreSQL + WebSocket |
| **مرسال Excel** | إحدى المنصات المزودة | Edge Functions + Supabase |
| **منصات أخرى** | أي منصة SMS لها API | REST / WebSocket / Realtime |

---

## 2. تدفق العمل

### 2.1 ربط منصة جديدة

```
1. فتح التطبيق → تبويب "المنصات"
2. اختيار "إضافة منصة" ← منصة جاهزة أو مخصصة
3. إدخال بيانات المصادقة:
   - JWT / API Key / Supabase Auth (حسب المنصة)
4. التطبيق يسأل المنصة:
   - "هذا هو جهازي: device_id + push_token + hardware_id"
   - المنصة تخزن البيانات وترد: "تم التسجيل"
5. ✅ المنصة جاهزة — تبدأ باستقبال الأوامر
```

### 2.2 إرسال رسالة عبر التطبيق

```
[أي منصة]
  │
  │ POST {to, message} → [جدول الأحداث]
  │                              │
  │                              ├── realtime push
  │                              │    (Supabase Realtime / WebSocket)
  │                              │
  └──→ [التطبيق]                 │
       │                         │
       │ يستقبل الحدث ←──────────┘
       │
       ├──→ ← 50 رسالة؟ → SIM card مباشر
       └──→ ≥ 50 رسالة؟ → API المنصة (hloov.com / أي API آخر)
       │
       └──→ تحديث الحالة: delivered / failed
```

### 2.3 الإشعارات

```
[أي منصة] → تدفع حدث → [Supabase Realtime] → [التطبيق]
                                                │
                                                ├──→ تحديث Dashboard
                                                └──→ Notification (app bg)
```

### 2.4 تحويل منصة الهدهد ويب ← مرسل Excel

هذا التدفق يسمح لمستخدم منصة الهدهد بالانتقال إلى مرسل Excel مع سياق المصادقة:

```
1. [منصة الهدهد ويب] ← المستخدم يضغط "إرسال عبر Excel"
2. [منصة الهدهد] تولّد JWT مخصص:
   {
     "sub": "hudhud_user_id",
     "email": "user@hudhud.com",
     "redirect_to": "https://mursal.hloov.com",
     "device_id": "known_device_uuid",
     "iat": ...,
     "exp": ... + 5 دقائق
   }
3. التوجيه: https://mursal.hloov.com/?token=<JWT>
4. [مرسل Excel] يستقبل JWT:
   - يتحقق من التوقيع عبر JWKS (https://api.hudhud.com/.well-known/jwks.json)
   - يستخرج user_id ← ينشئ user_link بين hudhud_user_id و supabase_user_id
   - يخوّل المستخدم دخول واجهة رفع Excel
5. المستخدم يرفع الملف ← تُنشأ حملة ← ترتبط بـ device_id
6. [تطبيق الموبايل] يستقبل التحديثات عبر Realtime
```

ملاحظة: لا استعلام API بين المنصات — فقط التحقق من JWT عبر المفتاح العام (JWKS).

---

## 3. المكونات الأساسية

### 3.1 في التطبيق (Expo)

- **إدارة المنصات**: إضافة/إزالة/تسجيل دخول/خروج لكل منصة
- **جدول الأحداث الموحد**: استقبال أوامر الإرسال من جميع المنصات
- **محرك إرسال ذكي**: يقرر الإرسال من SIM أو API حسب الحجم
- **سجل موحد**: كل الرسائل من كل المنصات في مكان واحد

### 3.2 في أي منصة ترغب بالاتصال

تحتاج إلى:
- جدول لتسجيل الأجهزة (device_id, push_token, hardware_id)
- آلية لدفع الأحداث (send_sms) إلى الأجهزة المسجلة
- استقبال تحديثات الحالة (delivered/failed)

---

## 4. نموذج الإيرادات

| المصدر | لمن | الآلية | الجدول |
|--------|-----|--------|--------|
| اشتراك المنصة | المنصة (B2B) | رسوم شهرية للظهور في قائمة المنصات | `platform_subscriptions` |
| اشتراك المستخدم | المستخدم (B2C) | مجاني 50 → Pro 500 = 4.99$ → غير محدود = 9.99$ | `user_subscriptions` |
| عمولة وساطة | المنصة (B2B) | 0.01$ لكل رسالة تمر عبر التطبيق | `platform_commissions` |

### 4.1 آلية العمولات بالتفصيل

```
1. عند كل رسالة تُرسَل بنجاح عبر التطبيق:
   - تسجيل في platform_commissions:
     platform_id, event_id, message_count=1, rate=0.01, total=0.01, status=pending

2. نهاية كل شهر:
   - تجميع كل commissions status=pending للمنصة
   - إنشاء فاتورة: total_commission × عدد الرسائل
   - إرسال الفاتورة للمنصة للدفع

3. بعد الدفع:
   - تحديث status → paid
   - إتاحة التقرير في Dashboard المنصة

4. إذا لم تدفع المنصة:
   - تعطيل إرسال الأحداث للمنصة
   - إشعار المنصة
```

### 4.2 جدول `platform_commissions`

```sql
CREATE TABLE platform_commissions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  platform_id UUID NOT NULL,
  device_id UUID,
  event_id UUID,
  message_count INT DEFAULT 1,
  rate NUMERIC(10,6) DEFAULT 0.01,
  total NUMERIC(12,4) NOT NULL,
  currency TEXT DEFAULT 'USD',
  status TEXT DEFAULT 'pending',  -- pending | billed | paid | void
  billing_period DATE,
  created_at TIMESTAMPTZ DEFAULT now(),
  paid_at TIMESTAMPTZ
);
```

### 4.3 ربط حساب العمولة عبر المنصات

```
- لكل منصة حساب commission منفصل
- المنصة ترى: "رسوم هذا الشهر: 45.20$"
- المستخدم لا يدفع العمولة — المنصة تدفعها
- العمولة تُخصم من رصيد المنصة أو تُفوتر شهرياً
```

---

## 5. مراحل التنفيذ

### المرحلة 1 — البنية التحتية
- [ ] جداول SQLite للتطبيق (platforms, events, send_log, settings)
- [ ] نظام إضافة/إزالة المنصات
- [ ] تخزين آمن لبيانات المصادقة

### المرحلة 2 — ربط المنصات
- [ ] تسجيل الدخول إلى منصة الهدهد من التطبيق
- [ ] تسجيل الدخول إلى مرسال Excel (Supabase Auth) من التطبيق
- [ ] الاشتراك في أحداث Realtime

### المرحلة 3 — محرك الإرسال
- [ ] إرسال عبر API (hloov.com / أي API آخر)
- [ ] إرسال عبر SIM (للحالات الصغيرة)
- [ ] التبديل التلقائي

### المرحلة 4 — نظام الاشتراكات
- [ ] حدود حسب الباقة
- [ ] بوابات دفع
- [ ] لوحة تحكم للمنصات

---

## 6. ملاحظات فنية

1. **كل منصة مستقلة تماماً** — لا تأثير بينها، كل منها يتحقق من توكنه بنفسه
2. **التخزين المحلي (SQLite)** — جميع المنصات والأحداث متوفرة حتى بدون إنترنت
3. **Hybrid Sending** — API للكميات الكبيرة، SIM للضرورة
4. **قابلية التوسع** — أي منصة جديدة = إضافة تكوين فقط
5. **اللغة** — كل الواجهة بالعربية (RTL)
