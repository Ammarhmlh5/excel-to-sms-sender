# خطة إصلاح بنية الإدارة — مرسال الهدهد

**تاريخ الإعداد:** 12 يوليو 2026  
**الحالة:** جاهز للتنفيذ  
**المدة المقدرة:** 3-5 أيام عمل

---

## جدول المحتويات

1. [الوضع الحالي](#الوضع-الحالي)
2. [الوضع المستهدف](#الوضع-المستهدف)
3. [خطوات التنفيذ](#خطوات-التنفيذ)
4. [الملفات المطلوب تعديلها](#الملفات-المطلوب-تعديلها)
5. [تغييرات قاعدة البيانات](#تغييرات-قاعدة-البيانات)
6. [قائمة الفحص والاختبار](#قائمة-الفحص-والاختبار)

---

## الوضع الحالي

### المشكلة
المشروع الحالي يخلط بين:
1. **لوحة تحكم المستخدم** — غير موجودة حاليًا
2. **لوحة تحكم المطور (Admin)** — موجودة لكن مصممة للمسؤول العام فقط

### البنية الحالية
```
التطبيق الرئيسي (port 5180)
├── / → Index.tsx (رفع Excel + إرسال SMS)
├── /auth → Auth.tsx (تسجيل دخول/خروج)
├── /admin → AdminLayout.tsx (لوحة المشرف فقط)
│   ├── /admin → AdminDashboard
│   ├── /admin/users → UsersManagement
│   ├── /admin/users/:id → UserDetail
│   ├── /admin/campaigns → CampaignsOverview
│   ├── /admin/logs → SmsLogsView
│   ├── /admin/api-keys → ApiKeysView
│   ├── /admin/devices → DevicesView
│   └── /admin/roles → RolesManagement
└── /reset-password → ResetPassword.tsx

التطبيق المنفصل (port 5181)
└── نسخة مستقلة من لوحة الإدارة
```

### المكونات الحالية في `src/admin/`
| الملف | السطر | الوظيفة |
|-------|--------|---------|
| `index.ts` | 9 | تصدير المكونات |
| `main.tsx` | 5 | نقطة الدخول المستقلة |
| `AdminApp.tsx` | 93 | تطبيق الإدارة المستقل |
| `AdminLayout.tsx` | 111 | تخطيط الشريط الجانبي |
| `AdminDashboard.tsx` | 101 | لوحة معلومات المسؤول |
| `UsersManagement.tsx` | 184 | إدارة المستخدمين |
| `UserDetail.tsx` | 453 | تفاصيل المستخدم |
| `CampaignsOverview.tsx` | 142 | عرض الحملات |
| `SmsLogsView.tsx` | 127 | سجلات SMS |
| `ApiKeysView.tsx` | 168 | مفاتيح API |
| `DevicesView.tsx` | 140 | الأجهزة |
| `RolesManagement.tsx` | 149 | إدارة الأدوار |

### المشاكل في الوضع الحالي
1. لا توجد لوحة تحكم **للمستخدم العادي** لإدارة حسابه
2. `/admin` مقصور على المسؤول العام فقط
3. لا يوجد تمييز بين "حسابي" و"إدارة النظام"
4. المستخدم لا يستطيع رؤية حملاته أو مفاتيحه أو أجهزته بشكل منفصل

---

## الوضع المستهدف

### البنية الجديدة
```
التطبيق الرئيسي (port 5180)
├── / → Index.tsx (رفع Excel + إرسال SMS)
├── /auth → Auth.tsx (تسجيل دخول/خروج)
├── /dashboard → DashboardLayout.tsx (لوحة تحكم المستخدم)
│   ├── /dashboard → MyCampaigns.tsx
│   ├── /dashboard/sms-logs → MySmsLogs.tsx
│   ├── /dashboard/api-keys → MyApiKeys.tsx
│   ├── /dashboard/devices → MyDevices.tsx
│   └── /dashboard/settings → AccountSettings.tsx
├── /super-admin → SuperAdminLayout.tsx (لوحة المطور فقط)
│   ├── /super-admin → SuperAdminDashboard.tsx
│   ├── /super-admin/users → UsersManagement.tsx
│   ├── /super-admin/users/:id → UserDetail.tsx
│   ├── /super-admin/campaigns → AllCampaigns.tsx
│   ├── /super-admin/logs → AllSmsLogs.tsx
│   ├── /super-admin/api-keys → AllApiKeys.tsx
│   ├── /super-admin/devices → AllDevices.tsx
│   └── /super-admin/roles → RolesManagement.tsx
└── /reset-password → ResetPassword.tsx
```

### الفرق بين المستخدم والمطور

| الميزة | المستخدم (`/dashboard`) | المطور (`/super-admin`) |
|--------|------------------------|------------------------|
| رؤية حملاته | ✅ حملاته فقط | ✅ كل الحملات |
| رؤية سجلاته | ✅ سجلاته فقط | ✅ كل السجلات |
| إدارة مفاتيحه | ✅ مفاتيحه فقط | ✅ كل المفاتيح + تفعيل/تعطيل |
| إدارة أجهزته | ✅ أجهزته فقط | ✅ كل الأجهزة |
| إدارة المستخدمين | ❌ | ✅ كاملة |
| إدارة الأدوار | ❌ | ✅ كاملة |
| عرض الإحصائيات | ✅ إحصائياته | ✅ إحصائيات النظام |

---

## خطوات التنفيذ

### المرحلة الأولى: إنشاء لوحة تحكم المستخدم (يوم 1-2)

#### الخطوة 1: إنشاء مجلد dashboard
```
src/components/dashboard/
├── MyCampaigns.tsx
├── MySmsLogs.tsx
├── MyApiKeys.tsx
├── MyDevices.tsx
├── AccountSettings.tsx
└── DashboardLayout.tsx
```

#### الخطوة 2: إنشاء DashboardLayout.tsx
```typescript
// src/components/dashboard/DashboardLayout.tsx
import { useState } from 'react';
import { Link, Outlet, useLocation } from 'react-router-dom';
import { useAuth } from '@/hooks/useAuth';
import { 
  Campaign, FileText, Key, Smartphone, 
  Settings, LogOut, Menu, X 
} from 'lucide-react';

const navItems = [
  { path: '/dashboard', label: 'حملاتي', icon: Campaign },
  { path: '/dashboard/sms-logs', label: 'سجل الإرسال', icon: FileText },
  { path: '/dashboard/api-keys', label: 'مفاتيح API', icon: Key },
  { path: '/dashboard/devices', label: 'أجهزتي', icon: Smartphone },
  { path: '/dashboard/settings', label: 'الإعدادات', icon: Settings },
];

export function DashboardLayout() {
  const { user, signOut } = useAuth();
  const location = useLocation();
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

  return (
    <div className="min-h-screen bg-gray-50 flex" dir="rtl">
      {/* الشريط الجانبي */}
      <aside className="w-64 bg-white border-l border-gray-200 hidden md:block">
        <div className="p-4">
          <h1 className="text-xl font-bold text-primary">لوحة تحكمي</h1>
        </div>
        <nav className="mt-4">
          {navItems.map((item) => (
            <Link
              key={item.path}
              to={item.path}
              className={`flex items-center gap-3 px-4 py-3 text-gray-700 hover:bg-gray-50 ${
                location.pathname === item.path ? 'bg-primary/5 text-primary border-r-2 border-primary' : ''
              }`}
            >
              <item.icon className="w-5 h-5" />
              <span>{item.label}</span>
            </Link>
          ))}
        </nav>
        <div className="absolute bottom-0 w-64 p-4 border-t">
          <div className="flex items-center gap-3 mb-4">
            <div className="w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center">
              <span className="text-primary font-bold">
                {user?.email?.charAt(0).toUpperCase()}
              </span>
            </div>
            <div className="flex-1 min-w-0">
              <p className="text-sm font-medium truncate">{user?.email}</p>
              <p className="text-xs text-gray-500">مستخدم</p>
            </div>
          </div>
          <button
            onClick={signOut}
            className="w-full flex items-center gap-2 px-4 py-2 text-red-600 hover:bg-red-50 rounded-lg"
          >
            <LogOut className="w-5 h-5" />
            <span>تسجيل الخروج</span>
          </button>
        </div>
      </aside>

      {/* المحتوى الرئيسي */}
      <main className="flex-1 overflow-auto">
        <div className="p-6">
          <Outlet />
        </div>
      </main>
    </div>
  );
}
```

#### الخطوة 3: إنشاء MyCampaigns.tsx
```typescript
// src/components/dashboard/MyCampaigns.tsx
import { useEffect, useState } from 'react';
import { useAuth } from '@/hooks/useAuth';
import { supabase } from '@/integrations/supabase/client';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { format } from 'date-fns';
import { ar } from 'date-fns/locale';

interface Campaign {
  id: string;
  name: string;
  status: string;
  contacts_count: number;
  sent_count: number;
  failed_count: number;
  created_at: string;
}

export function MyCampaigns() {
  const { user } = useAuth();
  const [campaigns, setCampaigns] = useState<Campaign[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchCampaigns();
  }, [user]);

  const fetchCampaigns = async () => {
    if (!user) return;
    
    const { data, error } = await supabase
      .from('campaigns')
      .select('*')
      .eq('user_id', user.id)
      .order('created_at', { ascending: false });
    
    if (!error && data) {
      setCampaigns(data);
    }
    setLoading(false);
  };

  const getStatusBadge = (status: string) => {
    const variants: Record<string, 'default' | 'secondary' | 'destructive' | 'outline'> = {
      completed: 'default',
      sending: 'secondary',
      failed: 'destructive',
      draft: 'outline',
    };
    return <Badge variant={variants[status] || 'outline'}>{status}</Badge>;
  };

  if (loading) {
    return <div className="text-center py-8">جاري التحميل...</div>;
  }

  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold">حملاتي</h1>
      
      <Card>
        <CardHeader>
          <CardTitle>قائمة الحملات ({campaigns.length})</CardTitle>
        </CardHeader>
        <CardContent>
          {campaigns.length === 0 ? (
            <p className="text-gray-500 text-center py-8">لا توجد حملات بعد</p>
          ) : (
            <div className="overflow-x-auto">
              <table className="w-full">
                <thead>
                  <tr className="border-b">
                    <th className="text-right py-3 px-4">الاسم</th>
                    <th className="text-right py-3 px-4">الحالة</th>
                    <th className="text-right py-3 px-4">الجهات</th>
                    <th className="text-right py-3 px-4">مرسلة</th>
                    <th className="text-right py-3 px-4">فاشلة</th>
                    <th className="text-right py-3 px-4">التاريخ</th>
                  </tr>
                </thead>
                <tbody>
                  {campaigns.map((campaign) => (
                    <tr key={campaign.id} className="border-b hover:bg-gray-50">
                      <td className="py-3 px-4 font-medium">{campaign.name}</td>
                      <td className="py-3 px-4">{getStatusBadge(campaign.status)}</td>
                      <td className="py-3 px-4">{campaign.contacts_count}</td>
                      <td className="py-3 px-4 text-green-600">{campaign.sent_count}</td>
                      <td className="py-3 px-4 text-red-600">{campaign.failed_count}</td>
                      <td className="py-3 px-4">
                        {format(new Date(campaign.created_at), 'dd MMM yyyy', { locale: ar })}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}
        </CardContent>
      </Card>
    </div>
  );
}
```

#### الخطوة 4: إنشاء باقي المكونات
(مشابه لـ MyCampaigns.tsx مع تعديل الاستعلام لكل جدول)

---

### المرحلة الثانية: تعديل المسارات الرئيسية (يوم 3)

#### الخطوة 5: تعديل src/App.tsx
```typescript
// src/App.tsx
import React from 'react';
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { AuthProvider, useAuth } from '@/hooks/useAuth';
import { useIsAdmin } from '@/hooks/useIsAdmin';

// صفحات المستخدم
import Index from './pages/Index';
import Auth from './pages/Auth';
import ResetPassword from './pages/ResetPassword';
import NotFound from './pages/NotFound';

// لوحة تحكم المستخدم
import { DashboardLayout } from '@/components/dashboard/DashboardLayout';
import { MyCampaigns } from '@/components/dashboard/MyCampaigns';
import { MySmsLogs } from '@/components/dashboard/MySmsLogs';
import { MyApiKeys } from '@/components/dashboard/MyApiKeys';
import { MyDevices } from '@/components/dashboard/MyDevices';
import { AccountSettings } from '@/components/dashboard/AccountSettings';

// لوحة تحكم المطور
import { SuperAdminLayout } from '@/components/super-admin/SuperAdminLayout';
import { SuperAdminDashboard } from '@/components/super-admin/SuperAdminDashboard';
import { UsersManagement } from '@/components/super-admin/UsersManagement';
import { UserDetail } from '@/components/super-admin/UserDetail';
import { AllCampaigns } from '@/components/super-admin/AllCampaigns';
import { AllSmsLogs } from '@/components/super-admin/AllSmsLogs';
import { AllApiKeys } from '@/components/super-admin/AllApiKeys';
import { AllDevices } from '@/components/super-admin/AllDevices';
import { RolesManagement } from '@/components/super-admin/RolesManagement';

// حماية المسارات
const ProtectedRoute = ({ children }: { children: React.ReactNode }) => {
  const { user, loading } = useAuth();
  if (loading) return <div>جاري التحميل...</div>;
  if (!user) return <Navigate to="/auth" replace />;
  return <>{children}</>;
};

const SuperAdminRoute = ({ children }: { children: React.ReactNode }) => {
  const { user, loading: authLoading } = useAuth();
  const { isAdmin, loading: adminLoading } = useIsAdmin();
  if (authLoading || adminLoading) return <div>جاري التحميل...</div>;
  if (!user) return <Navigate to="/auth" replace />;
  if (!isAdmin) return <Navigate to="/dashboard" replace />;
  return <>{children}</>;
};

const AppRoutes = () => (
  <Routes>
    {/* الصفحة الرئيسية */}
    <Route path="/" element={
      <ProtectedRoute><Index /></ProtectedRoute>
    } />
    
    {/* المصادقة */}
    <Route path="/auth" element={<Auth />} />
    <Route path="/reset-password" element={<ResetPassword />} />
    
    {/* لوحة تحكم المستخدم */}
    <Route path="/dashboard" element={
      <ProtectedRoute>
        <DashboardLayout />
      </ProtectedRoute>
    }>
      <Route index element={<MyCampaigns />} />
      <Route path="sms-logs" element={<MySmsLogs />} />
      <Route path="api-keys" element={<MyApiKeys />} />
      <Route path="devices" element={<MyDevices />} />
      <Route path="settings" element={<AccountSettings />} />
    </Route>
    
    {/* لوحة تحكم المطور */}
    <Route path="/super-admin" element={
      <SuperAdminRoute>
        <SuperAdminLayout />
      </SuperAdminRoute>
    }>
      <Route index element={<SuperAdminDashboard />} />
      <Route path="users" element={<UsersManagement />} />
      <Route path="users/:userId" element={<UserDetail />} />
      <Route path="campaigns" element={<AllCampaigns />} />
      <Route path="logs" element={<AllSmsLogs />} />
      <Route path="api-keys" element={<AllApiKeys />} />
      <Route path="devices" element={<AllDevices />} />
      <Route path="roles" element={<RolesManagement />} />
    </Route>
    
    {/* 404 */}
    <Route path="*" element={<NotFound />} />
  </Routes>
);

const App = () => (
  <BrowserRouter>
    <AuthProvider>
      <AppRoutes />
    </AuthProvider>
  </BrowserRouter>
);

export default App;
```

---

### المرحلة الثالثة: تعديل لوحة المطور (يوم 4)

#### الخطوة 6: نقل مكونات الإدارة إلى مجلد super-admin
```
src/components/super-admin/
├── SuperAdminLayout.tsx (نسخة معدلة من AdminLayout.tsx)
├── SuperAdminDashboard.tsx (نسخة معدلة من AdminDashboard.tsx)
├── UsersManagement.tsx (منقولة من admin/)
├── UserDetail.tsx (منقولة من admin/)
├── AllCampaigns.tsx (تعديل من CampaignsOverview.tsx)
├── AllSmsLogs.tsx (تعديل من SmsLogsView.tsx)
├── AllApiKeys.tsx (تعديل من ApiKeysView.tsx)
├── AllDevices.tsx (تعديل من DevicesView.tsx)
└── RolesManagement.tsx (منقولة من admin/)
```

#### الخطوة 7: تعديل SuperAdminLayout.tsx
```typescript
// src/components/super-admin/SuperAdminLayout.tsx
// تعديل الروابط لتبدأ بـ /super-admin بدلاً من /admin
const navItems = [
  { path: '/super-admin', label: 'لوحة المعلومات', icon: LayoutDashboard },
  { path: '/super-admin/users', label: 'المستخدمون', icon: Users },
  { path: '/super-admin/campaigns', label: 'الحملات', icon: Send },
  { path: '/super-admin/logs', label: 'سجلات SMS', icon: FileText },
  { path: '/super-admin/api-keys', label: 'مفاتيح API', icon: Key },
  { path: '/super-admin/devices', label: 'الأجهزة', icon: Smartphone },
  { path: '/super-admin/roles', label: 'الأدوار', icon: Shield },
];
```

---

### المرحلة الرابعة: تحديث التنقل (يوم 5)

#### الخطوة 8: تعديل Index.tsx
```typescript
// إضافة رابط لوحة تحكم المستخدم
import { Link } from 'react-router-dom';

// في المكون الرئيسي
<div className="flex items-center gap-4">
  <Link to="/dashboard" className="text-sm text-gray-600 hover:text-primary">
    لوحة تحكمي
  </Link>
  {isAdmin && (
    <Link to="/super-admin" className="text-sm text-red-600 hover:text-red-700">
      لوحة المشرف
    </Link>
  )}
</div>
```

#### الخطوة 9: تعديل index-admin.html (اختياري)
```html
<!-- يمكن حذف هذا الملف أو توجيهه إلى /super-admin -->
<!doctype html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <title>لوحة المشرف - مرسال الهدهد</title>
    <script>
      // توجيه تلقائي إلى /super-admin
      window.location.href = '/super-admin';
    </script>
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
```

---

## الملفات المطلوب تعديلها

### ملفات جديدة (إنشاء)
| الملف | الغرض |
|-------|--------|
| `src/components/dashboard/DashboardLayout.tsx` | تخطيط لوحة تحكم المستخدم |
| `src/components/dashboard/MyCampaigns.tsx` | حملات المستخدم |
| `src/components/dashboard/MySmsLogs.tsx` | سجلات SMS للمستخدم |
| `src/components/dashboard/MyApiKeys.tsx` | مفاتيح API للمستخدم |
| `src/components/dashboard/MyDevices.tsx` | أجهزة المستخدم |
| `src/components/dashboard/AccountSettings.tsx` | إعدادات الحساب |
| `src/components/super-admin/SuperAdminLayout.tsx` | تخطيط لوحة المطور |
| `src/components/super-admin/SuperAdminDashboard.tsx` | لوحة معلومات المطور |
| `src/components/super-admin/AllCampaigns.tsx` | كل الحملات |
| `src/components/super-admin/AllSmsLogs.tsx` | كل سجلات SMS |
| `src/components/super-admin/AllApiKeys.tsx` | كل مفاتيح API |
| `src/components/super-admin/AllDevices.tsx` | كل الأجهزة |

### ملفات موجودة (تعديل)
| الملف | التعديل المطلوب |
|-------|----------------|
| `src/App.tsx` | تعديل المسارات بالكامل |
| `src/pages/Index.tsx` | إضافة رابط لوحة تحكمي |
| `src/components/SendHistory.tsx` | تعديل الروابط |
| `src/admin/AdminLayout.tsx` | نسخه إلى super-admin |
| `src/admin/AdminDashboard.tsx` | نسخه إلى super-admin |
| `src/admin/UsersManagement.tsx` | نسخه إلى super-admin |
| `src/admin/UserDetail.tsx` | نسخه إلى super-admin |
| `src/admin/CampaignsOverview.tsx` | نسخه إلى super-admin |
| `src/admin/SmsLogsView.tsx` | نسخه إلى super-admin |
| `src/admin/ApiKeysView.tsx` | نسخه إلى super-admin |
| `src/admin/DevicesView.tsx` | نسخه إلى super-admin |
| `src/admin/RolesManagement.tsx` | نسخه إلى super-admin |

### ملفات يمكن حذفها (اختياري)
| الملف | السبب |
|-------|--------|
| `src/admin/` (المجلد بالكامل) | استبداله بـ `src/components/super-admin/` |
| `src/admin/main.tsx` | لم يعد مستخدمًا |
| `src/admin/AdminApp.tsx` | لم يعد مستخدمًا |
| `index-admin.html` | يمكن توجيهه إلى `/super-admin` |

---

## تغييرات قاعدة البيانات

### 1. تعديل RLS Policies للسماح لكل المستخدمين برؤية بياناتهم

```sql
-- campaigns: كل مستخدم يرى حملاته فقط
DROP POLICY IF EXISTS "Users view own campaigns" ON campaigns;
CREATE POLICY "Users view own campaigns"
  ON campaigns FOR SELECT
  USING (auth.uid() = user_id);

-- campaign_messages: كل مستخدم يرى رسائل حملاته فقط
DROP POLICY IF EXISTS "Users view own campaign messages" ON campaign_messages;
CREATE POLICY "Users view own campaign messages"
  ON campaign_messages FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM campaigns 
      WHERE campaigns.id = campaign_messages.campaign_id 
      AND campaigns.user_id = auth.uid()
    )
  );

-- api_keys: كل مستخدم يرى مفاتيحه فقط
DROP POLICY IF EXISTS "Users view own api keys" ON api_keys;
CREATE POLICY "Users view own api keys"
  ON api_keys FOR SELECT
  USING (auth.uid() = user_id);

-- device_push_tokens: كل مستخدم يرى أجهزته فقط
DROP POLICY IF EXISTS "Users view own devices" ON device_push_tokens;
CREATE POLICY "Users view own devices"
  ON device_push_tokens FOR SELECT
  USING (auth.uid() = user_id);

-- sms_logs: كل مستخدم يرى سجلاته فقط
DROP POLICY IF EXISTS "Users view own sms logs" ON sms_logs;
CREATE POLICY "Users view own sms logs"
  ON sms_logs FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM api_keys 
      WHERE api_keys.id = sms_logs.api_key_id 
      AND api_keys.user_id = auth.uid()
    )
  );
```

### 2. إنشاء Migration جديد

```sql
-- supabase/migrations/20260713000001_dashboard_rls_policies.sql

-- تحديث RLS policies للسماح لكل المستخدمين برؤية بياناتهم

-- 1. campaigns
ALTER POLICY "Users view own campaigns" ON campaigns
  USING (auth.uid() = user_id);

-- 2. campaign_messages
ALTER POLICY "Users view own campaign messages" ON campaign_messages
  USING (
    EXISTS (
      SELECT 1 FROM campaigns 
      WHERE campaigns.id = campaign_messages.campaign_id 
      AND campaigns.user_id = auth.uid()
    )
  );

-- 3. api_keys
ALTER POLICY "Users view own api keys" ON api_keys
  USING (auth.uid() = user_id);

-- 4. device_push_tokens
ALTER POLICY "Users view own devices" ON device_push_tokens
  USING (auth.uid() = user_id);

-- 5. sms_logs
ALTER POLICY "Users view own sms logs" ON sms_logs
  USING (
    EXISTS (
      SELECT 1 FROM api_keys 
      WHERE api_keys.id = sms_logs.api_key_id 
      AND api_keys.user_id = auth.uid()
    )
  );

-- 6. user_links
ALTER POLICY "Users view own links" ON user_links
  USING (auth.uid() = local_user_id);
```

---

## قائمة الفحص والاختبار

### اختبار لوحة تحكم المستخدم (`/dashboard`)
- [ ] تسجيل الدخول كمستخدم عادي
- [ ] التوجيه التلقائي إلى `/dashboard`
- [ ] عرض الحملات الخاصة بالمستخدم فقط
- [ ] عرض سجلات SMS الخاصة بالمستخدم فقط
- [ ] عرض مفاتيح API الخاصة بالمستخدم فقط
- [ ] عرض الأجهزة الخاصة بالمستخدم فقط
- [ ] تعديل إعدادات الحساب
- [ ] تسجيل الخروج

### اختبار لوحة تحكم المطور (`/super-admin`)
- [ ] تسجيل الدخول كمسؤول
- [ ] التوجيه التلقائي إلى `/super-admin`
- [ ] عرض كل المستخدمين
- [ ] عرض تفاصيل مستخدم معين
- [ ] عرض كل الحملات لجميع المستخدمين
- [ ] عرض كل سجلات SMS
- [ ] عرض كل مفاتيح API
- [ ] تفعيل/تعطيل مفتاح API
- [ ] عرض كل الأجهزة
- [ ] إدارة الأدوار

### اختبار الأمان
- [ ] المستخدم العادي لا يستطيع الوصول إلى `/super-admin`
- [ ] المستخدم العادي لا يرى بيانات مستخدمين آخرين
- [ ] المطور يرى كل البيانات
- [ ] RLS policies تعمل بشكل صحيح

### اختبار الأداء
- [ ] لوحة تحكم المستخدم تُحمّل بسرعة
- [ ] لا يوجد تأخير في عرض البيانات
- [ ] لا يوجد إعادة رسم غير ضرورية

---

## ملاحظات تقنية

### 1. استخدام Supabase Client المدمج
```typescript
// استخدام adminClient فقط في super-admin
import { supabase } from '@/integrations/supabase/client';

// في dashboard، استخدام supabase العادي (يعمل مع RLS)
const { data } = await supabase
  .from('campaigns')
  .select('*')
  .eq('user_id', user.id); // RLS يضمن عرض حملات المستخدم فقط
```

### 2. عدم الحاجة إلى Edge Functions جديدة
```typescript
// dashboard يمكنه استخدام supabase client مباشرة
// لا حاجة لاستدعاء Edge Functions
// RLS policies تتعامل مع التفويض
```

### 3. Lazy Loading للSuper Admin
```typescript
// يمكن استخدام React.lazy لتحميل super-admin عند الحاجة
const SuperAdminLayout = React.lazy(() => 
  import('@/components/super-admin/SuperAdminLayout')
);
```

---

## المدة المقدرة

| المرحلة | المدة | المخرجات |
|---------|-------|----------|
| المرحلة 1: إنشاء dashboard | يومان | 6 ملفات جديدة |
| المرحلة 2: تعديل المسارات | يوم | تعديل App.tsx + Index.tsx |
| المرحلة 3: تعديل super-admin | يوم | 8 ملفات منقولة/معدّلة |
| المرحلة 4: التنقل والاختبار | يوم | اختبار شامل |
| **المجموع** | **5 أيام** | بنية معمارية صحيحة |

---

**تاريخ الانتهاء:** 12 يوليو 2026  
**تم الإعداد بواسطة:** فاحص شامل للمشروع
